import React from 'react'

export default function Card(props) {
    
    const cardStyle = {
        padding: `${props.padding}px`,
    };
  return (
    <div className=" inline-flex bg-skin-base-2  border-1 border-skin-border-1 border-solid rounded-md  " style={cardStyle}>
        
        
        
    </div>
  )
}
